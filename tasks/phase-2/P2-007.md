# P2-007 — Public QA and Portfolio Release

## Objective

Perform final public-content and technical QA, make the smallest safe packaging
corrections, and prepare RescueLab's first factual portfolio release.

## Execution

- Actor: `CODEX_EXECUTOR`
- Surface: `CODEX_CLOUD`
- Branch: `phase-2/p2-007`
- Primary deliverables: `docs/public-qa.md`, final navigation/status corrections, and a release recommendation

## Required work

1. Audit every current-tree Markdown link and important portfolio claim.
2. Scan the current tree for credentials, private keys, tokens, personal paths,
   private marketplace/repository details, stale scaffolds, unsupported outcomes,
   and hidden incident-injection material outside its existing evidence artifacts.
3. Verify README navigation exposes the case studies, audit, reproducible demo,
   client intake/template, delivery playbook/template, scope limits, and CI evidence.
4. Confirm current status, task, backlog, handoff, PR references, and Phase 2
   progress are consistent.
5. Confirm the latest full GitHub Actions demonstration passes on the final head.
6. Review removal of `docs/workspace-architecture.md` and
   `docs/codex-upwork-mcp-workaround.md` from the public current tree because
   they contain private marketplace/workspace operations unrelated to the portfolio.
7. Do not rewrite historical incident reports merely to hide old local paths;
   document any residual history/current-tree limitation honestly.
8. Propose the smallest final edits only. Do not change application behavior or
   claim client, production, security, payment, revenue, or guaranteed outcomes.
9. Prepare factual release notes and recommend a semantic release tag. The tag
   and GitHub release are created only after independent review, merge, and
   passing final-head CI.

## Verification

- Check all current-tree Markdown links.
- Run a targeted secret/private-material/path scan and report exact remaining limitations.
- Run `npm ci`, `npm run check`, workflow/schema checks, and `git diff --check` where available.
- Require the repository's full GitHub Actions demonstration to pass on the final PR head.
- Confirm deletions are limited, recoverable from Git history, and justified by public scope.

## Delivery

Codex cloud must post every proposed new or changed file in full, list each
proposed deletion explicitly, and include the proposed next handoff in pull
request #18. Do not attempt to commit, push, merge, tag, or create a release.
Stop for independent review.
