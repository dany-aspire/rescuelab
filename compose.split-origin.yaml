services:
  api:
    ports:
      - "${SPLIT_API_PORT:-3000}:3000"
    environment:
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:5173}

  web:
    build:
      args:
        VITE_API_BASE_URL: http://localhost:${SPLIT_API_PORT:-3000}/api
